name: CI

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install test shells
        run: |
          sudo apt-get update
          sudo apt-get install -y zsh
      - name: Smoke tests
        run: ./tests/smoke.sh
